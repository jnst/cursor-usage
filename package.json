{
  "name": "@jnst/cursor-usage",
  "version": "0.13.0",
  "description": "Visualize Cursor usage-events CSV: terminal stats and a drag-and-drop local dashboard",
  "keywords": [
    "cli",
    "cursor",
    "dashboard",
    "usage"
  ],
  "homepage": "https://github.com/jnst/cursor-usage#readme",
  "bugs": {
    "url": "https://github.com/jnst/cursor-usage/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jnst/cursor-usage.git"
  },
  "bin": {
    "cursor-usage": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server/dev.ts",
    "test": "bun test",
    "test:browser": "bun run build && bun tests/dashboard-language.browser.ts",
    "typecheck": "tsc --noEmit",
    "check": "vp check",
    "fix": "vp check --fix",
    "precommit": "bun run typecheck && bun test",
    "prepush": "bun run check && bun run precommit && bun run build",
    "build": "bun run build.ts",
    "release": "bun run scripts/release.ts minor",
    "prepublishOnly": "bun run build",
    "prepare": "vp config --no-agent"
  },
  "dependencies": {
    "playwright-core": "^1.63.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "recharts": "^3.10.1",
    "typescript": "^7.0.2",
    "vite-plus": "^0.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
